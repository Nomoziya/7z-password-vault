;!@Install@!UTF-8!
Title="7-Zip Password Vault 26.03"
BeginPrompt="Install 7-Zip Password Vault 26.03?"
Progress="yes"
; A trailing backslash makes the module ask for the folder (default on D:, so the
; vault next to the program does not use the system drive). Without the backslash it
; would extract silently to the given path.
InstallPath="D:\7-Zip Password Vault\"
RunProgram="install.cmd"
;!@InstallEnd@!
